# 音乐数据 API（NeteaseCloudMusicApi）的出厂默认地址，构建时注入。
# 本地覆盖：创建 .env.local 填同名变量（.gitignore 的 *.local 规则会忽略它）。
# 应用内也可在 设置 → 网络 → API 服务 中运行时覆盖，无需重新构建。
VITE_API_BASE_URL=https://ncm-hjlepcuidu.cn-hangzhou.fcapp.run
-
